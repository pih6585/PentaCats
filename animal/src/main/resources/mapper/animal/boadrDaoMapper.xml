<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pentacats.animal.board.dao.BoardDaoMapper">

    <select id="findById" resultType="BoardEntity" parameterType="BoardEntity">
        SELECT * FROM board WHERE id = #{id}
    </select>

    <select id="findByAll" resultType="BoardEntity" >
        SELECT * FROM board
    </select>


</mapper>